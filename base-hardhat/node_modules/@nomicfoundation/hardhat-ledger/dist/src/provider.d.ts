import { EIP1193Provider, RequestArguments } from "hardhat/types";
import { ProviderWrapperWithChainId } from "hardhat/internal/core/providers/chainId";
import { LedgerOptions, EthWrapper, Paths } from "./types";
export declare class LedgerProvider extends ProviderWrapperWithChainId {
    readonly options: LedgerOptions;
    static readonly MAX_DERIVATION_ACCOUNTS = 20;
    static readonly DEFAULT_TIMEOUT = 3000;
    readonly paths: Paths;
    name: string;
    protected _eth: EthWrapper | undefined;
    static create(options: LedgerOptions, wrappedProvider: EIP1193Provider): Promise<LedgerProvider>;
    constructor(options: LedgerOptions, _wrappedProvider: EIP1193Provider);
    get eth(): EthWrapper;
    init(): Promise<void>;
    request(args: RequestArguments): Promise<unknown>;
    private _methodRequiresSignature;
    private _ethSign;
    private _personalSign;
    private _ethSignTypedDataV4;
    private _ethSendTransaction;
    private _derivePath;
    private _withConfirmation;
    private _toRpcSig;
    private _getNonce;
    private _requireControlledInit;
    private _requireControlledAddress;
}
//# sourceMappingURL=provider.d.ts.map